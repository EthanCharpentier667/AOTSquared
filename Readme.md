# ES-RS

AOT game made using EngineSquared. It is not a good exemple ! It's just a test...

## Build

Make sure [EngineSquared](https://github.com/EngineSquared/EngineSquared/) is cloned in `../EngineSquared`

Run:

```bash
xmake
```

To run the game, use

```bash
xmake run
```
